// Словарь для уровня 7 - Финальная битва (смешанные задачи)
export const mixedChallenges = {
  // Код
  code: [
    'function calculateSum(a, b) { return a + b; }',
    'const user = { name: "John", age: 30 };',
    'if (condition === true) { performAction(); }',
    'for (let i = 0; i < array.length; i++) { console.log(array[i]); }',
    'class Rectangle { constructor(width, height) { this.width = width; this.height = height; } }',
    'document.getElementById("button").addEventListener("click", handleClick);',
    'const result = await fetch("https://api.example.com/data");',
    'try { doSomething(); } catch (error) { console.error(error); }',
    'export default function App() { return <div>Hello World</div>; }',
    'const doubled = numbers.map(num => num * 2);'
  ],
  
  // Сложные слова
  complexWords: [
    'интерполяция', 'асинхронность', 'полиморфизм', 'инкапсуляция',
    'абстракция', 'рефакторинг', 'декомпозиция', 'сериализация',
    'аутентификация', 'авторизация', 'оптимизация', 'масштабирование',
    'интероперабельность', 'персистентность', 'идемпотентность', 'транзакция'
  ],
  
  // Математические выражения
  mathExpressions: [
    '(5 + 3) * 4 = 32', 'sqrt(144) = 12', '2^8 = 256', '15 % 4 = 3',
    '7*7 = 49', 'sin(90) = 1', '16/4 = 4', '3! = 6',
    '6 + 9 = 15', '76 - 42 = 34'
  ],
  
  // Короткие абзацы
  paragraphs: [
    'Программирование - это искусство создания инструкций для компьютера. Хороший код должен быть читаемым, эффективным и поддерживаемым.',
    'Алгоритмы определяют последовательность действий для решения задачи. Их эффективность измеряется временной и пространственной сложностью.',
    'Структуры данных - это способы организации информации в памяти компьютера. Правильный выбор структуры данных может значительно повысить производительность программы.'
  ]
};